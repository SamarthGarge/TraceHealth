# FastAPI backend URL (in dev, Vite proxies /api to this)
VITE_API_BASE_URL=http://localhost:8000

# Auth service URL (Better Auth — issues JWTs, handles Google OAuth)
VITE_AUTH_SERVICE_URL=http://localhost:4000
